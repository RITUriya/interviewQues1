import { DatepipeCheckPipe } from './datepipe-check.pipe';

describe('DatepipeCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new DatepipeCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
